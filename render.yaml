services:
  - type: web
    name: lurnic
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn src.lurnic.api:app --host 0.0.0.0 --port 10000
    envVars:
      - key: GOOGLE_API_KEY
        sync: false